// 写一个处理由空格分隔的字符串的函数：
// vector<string> split(const string& s, const string& w)，
// 返回一个包含其子字符串的数组
// “空格”是普通的空格加上参数w里的字符

export module Ex_10;

import PPP;
using namespace std;

//vector<string> split(const string& s, const string& w)
//{
//	return;
//}

export void Ex_10();